{
  "name": "web-3-bloombeads",
  "version": "2.0.0",
  "description": "web 3, project 1, bloombeads",
  "main": "src/index.html",
  "scripts": {
    "sync": "browser-sync start -s \"src\" -f \"src/**/*.*\"",
    "sass": "node-sass -w --source-map=true src/styles.scss src/styles.css",
    "start": "npm-run-all -p sync sass",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Julia Schantz",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.24.7",
    "node-sass": "^4.14.1",
    "npm-run-all": "^4.1.3"
  }
}
